import { FormatarNotaPipe } from './formatar-nota.pipe';

describe('FormatarNotaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarNotaPipe();
    expect(pipe).toBeTruthy();
  });
});
